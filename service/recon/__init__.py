"""Keystone reconciliation service.

`recon` is the single Python package for the service: adapters, normalization,
entity resolution, invariants, the reconciler, and the graded suite all live here.
This module intentionally holds nothing but package metadata so that importing
`recon` stays free of side effects.
"""

__version__ = "0.1.0"

__all__ = ["__version__"]
